# Superseded: use the Gamepad Tools prompt

The user replaced the former studio/fork/port scope with an independently implemented Babylon.js/WebGPU input-binding toolkit for foss-earth and 0sfs.

Use [gamepad-tools-agent-prompt.md](gamepad-tools-agent-prompt.md). It covers live controller feedback, optional 3D visualization and keyboard/gamepad action bindings. Games, gallery, streaming/OBS and full DualSense Studio parity are outside the task.

Repository: [Felipegalind0/gamepad-tools](https://github.com/Felipegalind0/gamepad-tools). The former gamepad-studio repository was deleted.
